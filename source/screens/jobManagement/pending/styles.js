import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    // marginTop: 30,
  },
  bookedJobsList: {
    backgroundColor: "#FFF",
    padding: 20, paddingBottom: 0
  },
});
