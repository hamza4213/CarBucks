import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  checkBox: {
    borderWidth: 1,
    borderRadius: 5,
    // borderColor: 'white',
    height: 18,
    width: 18,

    // // marginTop: 5,
    justifyContent: 'center',
    alignItems: 'center',
    // paddingRight: 5,
    // paddingLeft: 5,
  },
});
