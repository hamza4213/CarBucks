import {StyleSheet} from 'react-native';
import {btnBackgroundGray} from '../../../constants/colors';

export default StyleSheet.create({
  button: {
    borderRadius: 15,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: btnBackgroundGray,
  },
  grdBtn: {
    color: 'white',
  },
});
